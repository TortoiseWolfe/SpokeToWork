/** Barrel re-export — keeps existing imports working. */
export {
  type AnyCompany,
  isUnifiedCompany,
  getCompanyRowId,
  isCompanyOnActiveRoute,
  filterCompanies,
} from './table-filter';
export { sortCompanies } from './table-sort';
